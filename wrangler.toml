# Cloudflare Pages — deploy with: npm run deploy:cf
# (requires: wrangler login, and a Pages project; first deploy may create the project)
name = "vtt"
compatibility_date = "2025-01-01"
pages_build_output_dir = "./dist"
